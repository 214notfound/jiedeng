# 村口子场景交接

## 第一部分：人必须看懂（给高冰轩）

**村口光点先进入子场景，子场景里再点人物光点才开始对话。进入和返回不记录剧情进度；对话真正读完后仍走原完成接口。**

**主场景五个坐标沿用已确认版本；三个子场景采用杨梦建议坐标。五个主场景点依旧按原剧情条件分阶段出现，不同时强行开放。**

| 位置 | 对象/动作 | 坐标 | 交互 |
|---|---|---|---|
| 村口 | village-decline | 15,30 | item：原调查与反馈 |
| 村口 | su-he-notice | 85,30 | item：原调查后详情 |
| 村口 | shopkeeper-inquiry | 22,50 | scene：进入小卖部 |
| 村口 | holdout-inquiry | 78,50 | scene：进入拒签户家 |
| 村口 | elder-inquiry | 50,38 | scene：进入路边石凳 |
| village-shop | villager-1 | 50,45 | conversation |
| village-holdout-house | villager-2 | 50,42 | conversation |
| village-elder-stone | villager-3 | 50,48 | conversation |

**素材已更新：** 仅采用杨梦最新的 `子场景(1).zip`，三张实际图片均已核实为1280×720，与草图比例一致；此前的尺寸不一致事项关闭。三个建议点继续保留。正式图中人物已存在，对话时不再叠加旧立绘，图片完整显示、无裁切。

**给高冰轩：** 子场景仍属于 exploration，点击人物继续走 reading。场景上方已有“返回村口”按钮；如需移动到公共顶栏，请在页面侧协作接入。详情、背包、小游戏与阅读仍由全局锁背景。场景图片内有人物这一规则仅覆盖这三个村口子场景，祠堂和老宅保留原独立人物层。

**请高冰轩确认三件事：**

1. 返回入口：现有“返回村口”在探索模块的场景上方，可直接使用。确认保留此位置还是需要公共顶栏方案；若移到顶栏，先对齐调用入口，避免生成两个按钮。
2. 视觉：在390/768/1280宽度下检查三张子场景光点位置、人物辨识度和阅读面板。阅读时光点与返回按钮不可操作，移动端正文和操作按钮可见。
3. 合并验收：主场景进子场景不应立刻对话；第二次点击才对话。未读完关闭保留当前子场景，返回村口可继续找其他人；第三人完成后按全局最新响应继续。背包/详情关闭沿用原返回链。

**无需重新索要Host，也无需增加全局页面状态。** 本轮是E1展示与E3入口的变化；E2调查、E4详情背包、E5成就继续沿用现有逻辑。素材尺寸项已经解决，剩余是页面视觉确认与合并后验收。

## 第二部分：AI接入细节与边界

### 最新素材映射

| ZIP内文件 | 仓库文件（assets/images/exploration/scenes/） | 尺寸 |
|---|---|---|
| 子场景/子场景-小卖部老板.png | village-shop.png | 1280×720 |
| 子场景/子场景-拒签户.png | village-holdout-house.png | 1280×720 |
| 子场景/子场景-年老村民 .png | village-elder-stone.png | 1280×720 |

老人源文件名在扩展名前含空格，运行时使用上表稳定文件名。旧包不再作为这三张资源的来源。使用现有16:9容器、contain及同容器百分比热点，不新增裁切规则。

数据位于 `assets/js/exploration/data/village-subscenes.js`。`projectVillageScene(view, layout, selectedId)` 将已授权的村口交互投影为入口或单人物子场景，不增加剧情命令或事实。

业务 `sceneId` 仍为 village，传给 getReadingInput/completeReading 的参数不变。新增三个 sceneId 仅用于展示投影及舞台 data-scene-id，不能写入 storyCheckpoint、locations 或全局存档。sceneVariant 仍为 default。

保留既有 item/conversation 枚举，新增仅供展示路由的 scene；素材文档建议的 detail/dialogue 不用于重命名当前业务合同。

返回按钮只清除当前模块临时选景并还原入口焦点。取消对话保留子场景；失败保留原任务便于重试。最新状态不再包含该对话动作时回到主场景。重载按正式存档重建业务状态，临时子场景不保存。

不调用 enterStory，不提交换景事件，不改全局状态机，不截获 ESC/浏览器返回；覆盖层仍走 closeOverlay。可选追问仍执行方案A，不新增记忆事实。

验收以390/768/1280为团队既有要求；不把坐标适合320宽直接等同于整个页面支持320。

最新素材替换后正式页面回归已覆盖三个子场景的两次点击、切换不改状态、返回后访问下一人物、人物不重复叠加、三视口截图及原E1–E5完整链路。返回按钮已补深色背景提升对比度。素材以本次用户确认的最终包为准；合并后由高冰轩复核视觉与原路径，关闭本轮交接。
