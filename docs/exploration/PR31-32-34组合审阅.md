# PR #31 / #32 / #34 组合审阅

## 结论

组合实现已通过本轮自动化验证，可交用户批准发布。远端尚未合并、推送。审阅分支：`codex/pr31-32-34-review`。

基线是远端 release-v2 的 `f244876a29a0909bcb8e3a187a415e8defe974ae`，它撤回了 `cc3ff31` 的统一阅读布局。不得把已撤回的 data-content-kind 布局逻辑重新引入。

| PR | 检查的头提交 | 本地处理 |
|---|---|---|
| #31 | 9fbdbe1baf802fb75f76ad6a62729bd072cc6251 | 保留唯一关闭按钮，保留 detail-card__content；补嵌套详情的滚动与图片适配 |
| #32 | a344773804e671bb1805df08bff6498ccce4c0e5 | 在 #31 上合入，无文本冲突 |
| #34 | cdbafce2c634b49a9623d7393defbb1548c3a776 | 在最新基线上无文本冲突，保留 actionStatus 与 aria-busy 清理、阅读失败重试实现 |

## 修复理由

#31 使用的详情 CSS 原先适配与卡片并列的 exploration-module，但正式库存视图优先挂载到卡片内 detail-card__content。简单取任一侧会丢掉内容挂载点，或使内容继承 pointer-events:none、图片限制落在无效选择器上。本次保留正式挂载结构，嵌套卡片允许鼠标交互，内容在卡片内部滚动，图片完整显示；关闭按钮位于滚动区域之外。

#34 在旧基线上的 clear() 冲突已随远端撤回消失。本次依据最新基线合并，没有把已经撤回的内容类型逻辑拼回去。

## 验证

- 精确运行 *.test.js / *.test.mjs：114通过，0失败。
- test/exploration/browser.cjs：正式 game.html 路径的 E1–E5、11节点、390/768/1280视口、Host拒绝重试、阅读、详情关闭/浏览器返回、地图、成就、保存恢复通过。
- 增补真实鼠标滚动验证：详情加入长文本后滚轮能改变内容 scrollTop，恢复内容后关闭按钮可点击。
- git diff --check 通过。

浏览器脚本以独立浏览器上下文运行，使用 game.html?mode=new&debug=1 注入失败。此记录不等于人工手机验收，也不包含队友尚未提交的变更。

## 边界与批准后的动作

没有改 Host、state、storage、剧情数据、热点数据；关闭及覆盖层继续复用原控制器。保留PR作者的改动与提交历史。原 exploration-layout-V2 工作区不改动。

用户批准后，重新核对远端基线及三个头提交，发布这个组合修复分支并走团队PR合并流程。推荐以组合分支保留三个PR的原始提交和修复，而不是直接对仍冲突的 #31 点击合并。若远端有新提交，应先更新演算和必要回归。

子场景更新（当前 exploration-layout-V2 的34a9beb）不在本次三PR组合中，高冰轩后续视觉校对也仍待交付；它们合入后还需相应回归。本报告不据此宣布整个V2最终验收结束。
