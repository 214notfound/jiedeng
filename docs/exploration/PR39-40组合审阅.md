# PR39、PR40 组合审阅与约束

**本次仅组合 PR39、PR40，不包含 PR28。测试通过，可提交用户审阅。**

基线 release-v2：735ca48（包含 PR38）。PR39：1fc9591；PR40：a321e55。

## 调整与验证

两个 PR 自动合并，无文本冲突。PR40 删除的 CSS 中存在仍有效的声明，导致详情长内容不能滚动。组合修正将卡片背景、边框、阴影、overflow、内容高度和内边距、嵌套内容配色及标题避让并入保留规则，不恢复整段重复声明。保留 PR39 的桌面场景高度适配。

生产文件仅修改 assets/css/game.css。本次用户要求调整组合；此文件属高冰轩页面职责，合并前应由他复核保留规则。未修改 Host 或页面状态接口。

- 全量 Node 测试：116/116 通过。
- test/exploration/browser.cjs：正式 game.html 的 E1–E5、11 Nodes、390/768/1280、Host 失败重试、阅读完成、详情滚动及关闭/浏览器返回、地图、成就、保存恢复通过。
- 浏览器链路含 debug=1，用于失败注入；不能据此宣称无调试正式 URL 的所有异常路径均已验收。
- git diff --check 通过。
- PR28 的地图资源失败保护及其测试不属于本次结果，也未声明已经解决。

## 必须保持的规范

1. 以 V2 界面状态约定、V2.1 全局交接及当前正式实现为准。Host 来自 game-flow.js 和 game-page-controller.js；不套用旧 V1 页面接口。
2. 只有 reading/exploration/detail/inventory/minigame 五个全局状态，由控制器决定显隐、锁定、焦点和返回链。CSS 只消费状态。
3. 详情关闭复用全局入口；关闭不产生事实，背包进入详情后先返回背包。保留唯一可操作层，禁止穿透点击。
4. 场景图和百分比热点共用 16:9 坐标空间；本次不修改坐标、素材或剧情显示条件。
5. 村口先点光点进入子场景，再点人物光点打开阅读；子场景属于临时展示，不新增全局状态或存档字段。
6. 翻页不提交事实；真实读完调用现有 completeReading。Host 明确失败允许重试，其他保护锁保持现状；可选追问采用方案 A，不记录新增记忆事实。
7. 全局、存档、剧情和探索业务不因页面样式调整而改变。图片、容器、坐标问题分别由杨梦、高冰轩、卢正松处理。

新远端分支 codex/pr39-40-review 供审阅，不自动合并 release-v2。旧 codex/pr28-39-40-review 由本地同名分支保留备份；删除其远端引用只清理旧审阅入口，不删除原 PR28、PR39、PR40 分支。
