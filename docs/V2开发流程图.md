# 《借灯》V2 开发流程图

这张图按依赖关系阅读，不按日期阅读。图的上方是最终要交付的玩家体验，向下倒推必须先准备的接口、素材和 V1 基础；实际开发从下往上推进。

## 1. 总体依赖图

```mermaid
flowchart TB
    V2["V2 最终验收<br/>剧情阅读与探索分离<br/>鼠标点击发光热点<br/>祠堂→村口→陈家老宅可连续游玩<br/>存档/继续/成就/地图拼图可用<br/>页面不显示 Node、事件名、命令 ID 等内部信息"]

    I4["共同验收：完整玩家路径<br/>新游戏→阅读→探索调查/对话→地图拼图→奖励→保存→继续<br/>罗晨菲主持；于知让核对剧情事实；卢正松核对交互事件；<br/>高冰轩核对页面状态；杨梦核对场景与裁切"]
    I3["正式跨模块集成<br/>一个 game.html 里共用同一 game-flow/state/storage<br/>reading / exploration / detail / inventory / minigame 互斥切换"]
    I2["接口交接确认<br/>《V2 阅读接口交接》<br/>《V2 场景美术与热点交接》<br/>《V2 界面状态切换约定》"]
    I1["前置交付物齐备<br/>页面容器清单、剧情真实样例、对象清单、标记草图、<br/>场景图片、热点坐标、对话样例、状态映射"]

    F["V1 可复用基础<br/>state.js / storage.js / navigation.js / game-flow.js<br/>game-contract.js、剧情 facts/checkpoint、地图拼图、成就、存档"]

    V2 --> I4 --> I3 --> I2 --> I1 --> F

    subgraph P["可并行准备的五条工作线"]
      P1["罗晨菲｜全局接线<br/>状态枚举与切换表<br/>正式 host 入口和返回结构<br/>玩家反馈文案清单"]
      P2["高冰轩｜页面壳与阅读<br/>game.html 挂载点<br/>reading/detail/inventory/minigame 容器<br/>阅读组件 API"]
      P3["于知让｜剧情样例<br/>prologue-wake 真实展示数据<br/>11 个 Node 的展示/等待清单<br/>action 完成/失败语义"]
      P4["卢正松｜探索与业务样例<br/>三场景对象/人物表<br/>action、事实、百分比坐标<br/>NPC 对话、详情、背包样例"]
      P5["杨梦｜美术前置<br/>按对象 ID 标记三场景草图<br/>冻结构图后输出三张正式背景"]
    end

    F --> P1
    F --> P3
    F --> P4
    P4 --> P5
    P1 --> P2
    P3 --> P2
    P4 --> P2

    P1 --> I1
    P2 --> I1
    P3 --> I1
    P4 --> I1
    P5 --> I1

    subgraph G["必须共同确认的交接门"]
      G1["G1 阅读交接<br/>于知让给真实剧情样例<br/>高冰轩渲染到末尾并只回调一次<br/>罗晨菲确认回调进入 game-flow"]
      G2["G2 场景/热点交接<br/>卢正松对象 ID ↔ 杨梦草图编号 ↔ 高冰轩图片容器<br/>三方叠加检查后才能冻结坐标"]
      G3["G3 事件链交接<br/>点击物体→OBJECT_INVESTIGATED<br/>点击人物→阅读完成→NPC_TALKED<br/>点击地图→MAP_PUZZLE_COMPLETED"]
      G4["G4 存档与反馈交接<br/>调查/对话/拼图完成后保存并恢复<br/>失败只显示玩家能懂的中文提示"]
    end

    I2 -. 阅读接口交接 .-> G1
    I2 -. 场景热点交接 .-> G2
    I3 -. 事件链检查 .-> G3
    I3 -. 存档反馈检查 .-> G4
```

图中关系的含义：

- **先做**：在 V1 基础之上，先产出真实样例、对象清单、页面容器和场景草图；没有这些输入，接入方不得自行猜字段或坐标。
- **可并行**：罗晨菲整理全局接线，高冰轩搭页面壳，于知让整理剧情，卢正松整理探索对象，杨梦根据对象清单画标记草图。这些工作互相提供小型输入，但不需要等到所有代码完成才开始。
- **必须等待**：热点坐标要等“对象清单 + 标记草图 + 实际图片容器比例”三者对齐；阅读组件要等剧情样例和 NPC 对话样例；正式集成要等页面容器、阅读组件、探索模块和场景图都能挂载。
- **共同验证**：每个交接门都由提供方给真实输入，使用方在正式 `game.html` 点击验证，再由罗晨菲确认事件是否经过 `game-flow → state → storage`。

## 2. 五人协作泳道

```mermaid
flowchart LR
  subgraph L["罗晨菲｜全局、状态、正式集成"]
    L1["定义五种界面状态<br/>及进入/返回/失败规则"]
    L2["提供正式 host<br/>统一接收模块请求"]
    L3["接入 state / storage<br/>保存、恢复、错误路由"]
    L4["组织跨模块回归<br/>记录问题归属"]
    L1 --> L2 --> L3 --> L4
  end

  subgraph H["高冰轩｜页面与阅读组件"]
    H1["提交容器 ID 与显示规则"]
    H2["搭 game.html 页面壳<br/>背景、阅读、覆盖层、小游戏"]
    H3["按真实样例实现阅读组件"]
    H4["锁定背景点击、处理关闭返回<br/>做 390/768/1280 宽度检查"]
    H1 --> H2 --> H3 --> H4
  end

  subgraph Y["于知让｜剧情"]
    Y1["提交剧情真实样例<br/>旁白/对白/系统文字/action"]
    Y2["确认阅读完成与 action 提交边界"]
    Y3["回归 11 个 Node、事实与阶段终点"]
    Y1 --> Y2 --> Y3
  end

  subgraph R["卢正松｜探索、对话、背包、成就"]
    R1["提交对象/人物清单<br/>action、事实、坐标、条件"]
    R2["接入鼠标热点调查与 NPC 对话"]
    R3["接入详情、背包、成就并回归"]
    R1 --> R2 --> R3
  end

  subgraph M["杨梦｜场景美术"]
    M1["按对象 ID 画三场景标记草图"]
    M2["冻结构图后输出正式背景"]
    M3["检查裁切、遮挡、可辨认性"]
    M1 --> M2 --> M3
  end

  Y1 -->|"真实字段样例"| H3
  R1 -->|"对象编号/坐标依据"| M1
  M1 -->|"标记草图"| R2
  M2 -->|"正式图片"| H2
  H1 -->|"共同容器比例"| R1
  H3 -->|"完成回调"| L2
  R2 -->|"OBJECT_INVESTIGATED / NPC_TALKED"| L2
  L2 -->|"最新剧情响应/状态"| H4
  L3 -->|"保存/恢复结果"| H4
  H4 -->|"页面可操作结果"| L4
  Y3 -->|"剧情事实核对"| L4
  R3 -->|"交互与成就核对"| L4
  M3 -->|"视觉问题清单"| L4
```

## 3. 每个交接点要交什么、谁来确认

| 交接点 | 提供方 → 使用方 | 必须确认的具体内容 | 通过标准 |
|---|---|---|---|
| 阅读样例 | 于知让 → 高冰轩、罗晨菲 | 文本块类型、说话人、顺序、action ID、结束/等待含义 | 高冰轩能逐段显示；末尾只回调一次；罗晨菲收到合法 action |
| NPC 对话 | 卢正松 → 高冰轩、罗晨菲、于知让 | `conversationId/npcId`、对白段落、完成/取消事件 | 完成前不提交 `NPC_TALKED`；完成后事实和 Node 均正确 |
| 场景对象 | 卢正松 → 杨梦、高冰轩 | 对象/人物 ID、名称、出现条件、必需性、百分比坐标 | 草图编号、代码坐标、图片对象一一对应 |
| 场景图片 | 杨梦 → 高冰轩、卢正松 | 文件路径、尺寸/比例、对象位置、文字安全区 | 图片替换后热点仍命中原对象，无关键遮挡 |
| 界面状态 | 罗晨菲 → 高冰轩、于知让、卢正松 | 进入、取消、失败、完成、返回目标；覆盖层打开时禁止背景点击 | 任一入口只显示一个主状态；关闭后回到正确状态 |
| 保存恢复 | 罗晨菲 → 全体 | 保存前后 `storyCheckpoint/facts`、坏档/无档提示 | 刷新或继续游戏后页面、事实、检查点一致；失败不伪装成新游戏 |
| 最终验收 | 罗晨菲主持 → 全体 | 同一点击路径的页面、事件、事实、存档、视觉结果 | 三阶段路径可通关；所有问题有唯一归属并完成复测 |

## 4. 当前版本到 V2 的倒推关系

- **现在已有**：V1 的状态、存档、事件合同、剧情检查点、地图拼图和成就基础，作为最底层依赖保留。
- **先补齐输入**：剧情真实展示样例、NPC 对话样例、三场景对象清单、对象编号草图、页面容器 ID 和界面状态表。
- **再做模块实现**：高冰轩实现页面壳与阅读组件；卢正松实现点击热点及业务视图；杨梦完成正式场景图；于知让完成剧情回调适配；罗晨菲提供统一 host 并接线。
- **最后做共同联调**：沿“点击热点/人物/地图 → 页面状态 → 外部事件 → 剧情响应 → 保存恢复”的同一条链验证，直到 V2 最终验收条件全部满足。

相关任务卡详见 [项目需求文档v2.md](./项目需求文档v2.md)。
