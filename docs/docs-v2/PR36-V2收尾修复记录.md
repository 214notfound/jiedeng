# PR36 / V2 收尾修复记录

## 本轮边界

- 本轮不修改 `assets/css/game.css`。
- 不修改剧情 Node、事实 ID、命令、存档结构、全局状态或页面结构。
- 仅修复统一阅读出口中的文本分段，并补充页面层回归测试。

## 已修复

`assets/js/core/game-ui.js` 现在会按换行拆分阅读文本，并逐行识别 `【角色】` 标签。多角色对白、对白后的无标签旁白和连续叙述会分别渲染在统一的 `#game-story` 阅读出口内，避免整段粘连。

无标签行不会写入场景、热点或反馈层；旁白仍留在对话框中，不新增独立旁白出口。

## 回归验证

- 同一行中以空格连接的连续角色标签也会被拆分；句中普通括号不会误判为角色标签。
- `test/core/game-ui.test.mjs`
- `test/core/reading-contract.test.mjs`
- `test/exploration/conversation-reading-integration.test.mjs`

以上定向测试通过，共 17 项。

## 尚未处理

以下项目按约定保留到后续 `game.css` 专项，不属于本轮提交：

- 游戏画面铺满视口；
- 对话框高度调整；
- 旁白与对白字号统一及对白字号下调。

这些项目完成后，仍需在 390 / 768 / 1280 三档进行最终浏览器验收。
