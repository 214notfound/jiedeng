% ============================================================
% 第九章 · 个人总结
% 对应大纲：# 九、个人总结
%
% 每位组员的总结写成一个 \section，姓名作为小节标题，顺序与 tex/info.tex
% 的成员名单一致。各节内部结构统一：
%   一行题头（项目 · 时间）→ 引言 → \subsection 按 V1 至 V4 分述
%   → \subsection{回看这三周} → 相关 PR → 依据说明
%
% 正文由各成员自己提供，本章只做排版，不改写文字内容。
%
% 当前进度：于知让、高冰轩两节已完成；其余三节留空，用 \pending 标出（红色，
% 定稿前全局搜索 \pending 确认清空）。
%
% GitHub 账号与成员的对应关系（从 PR 作者与 PR 内容核对得出）：
%   ICEXUAN-2707      高冰轩   31 个 PR
%   LS-commits-Byte   卢正松   16 个 PR
%   214notfound       罗晨菲   11 个 PR
%   z77530464-star    于知让   10 个 PR
%   ym-youxi          杨梦      5 个 PR
%
% 相关 PR 用 \href{链接}{文字} 排版，# 号在显示文字里要写成 \#。
% 依据说明用 \small 加浅灰，不要用 \itshape：中文在 ctex 下没有真正的
% 斜体，只会得到难看的伪斜体或不起作用。
% ============================================================

\chapter{个人总结}

本章收录五名成员的个人总结。各节按同一结构组织，分述各人在四个版本中承担的工作、遇到的问题与收获。

\section{罗晨菲}

\pending{本节待补充：按 V1 至 V4 分述承担的工作，末尾列相关 PR}

% 罗晨菲的可用事实（供撰写时参考，取自各代需求文档与第五章）：
%   V1  全局与存档
%   V2  全局状态与正式集成；文件边界为 assets/js/core/navigation.js、
%       game-flow.js 及必要的全局状态接口；G1 界面状态协调、G2 正式宿主
%       接线、G3 保存与恢复兼容、G4 集成验收、G5 玩家反馈与内部信息隔离
%   V3  完成全局模块的对接移交；此后与于知让同期参加数学建模竞赛
%   V4  全局状态协调；素材、视频与音效的接入

\section{杨梦}

\pending{本节待补充：按 V1 至 V4 分述承担的工作，末尾列相关 PR}

% 杨梦的可用事实（供撰写时参考）：
%   V1  入口页与介绍页
%   V2  场景美术与视觉验收；文件边界为三张场景背景与视觉规范，不修改
%       热点坐标、业务状态、剧情内容和页面切换代码；A1 构图与热点可画性、
%       A2 三场景正式背景、A3 视觉验收与修正
%   V3  场景美术、视觉与听觉；剧情展示与剧情回归；冻结场景结构、制作背景、
%       视觉验收、配置音乐；核对五条结局路径
%   V4  页面切换转场素材；剧情视频（亮灯、结局）素材与插入；音频素材与插入

\section{于知让}

{\small\color{black!55}《借灯》网页交互故事 · 2026 年 9 月 3 日—9 月 20 日\par}

我在项目中承担两类工作。一类是模块开发，从账户到剧情引擎，再到 V4 的剧情文本定稿；另一类在方法层面，以 PR 驱动需求迭代的开发方式由我在立项阶段提出。

三周下来，两点体会最深。原创剧情要在一个受限的游戏形式里成立，比写故事本身难。开发方式提出来只是一句话，让它真正跑起来，还需要补齐一整层当时没有意识到的东西。

\subsection{V1：账户模块、剧情引擎与开发方式}

V1 我负责账户与剧情两个模块。账户部分把登录与注册页迁入 assets 目录结构；剧情部分从零实现引擎的第一版，包括节点推进、动作提交与状态记录，并把内容与引擎分开，数据文件存放剧情内容，引擎只负责推进和渲染，不含页面实现。这个划分在 V4 得到验证，46 处文本修订全部落在数据文件上，引擎代码没有改动。

开发方式也是在这一代提出的。9 月 3 日确定以 PR 驱动需求迭代，需求先写成文档，评审确定边界，再拆成若干可独立交付的 subPR 并行开发。提出时的考虑是五个人的工作时间不连续，需要一种能把并行工作隔开的单位。

问题出在执行上。当时我以为把需求讲清楚就足够，没有同时确定文件归属、模块之间的通信字段和每一块的改动边界。结果是 V1 后期团队接口约定草案被整体废弃，改为按模块拆分的多份文档；同期还有一批已经合并的页面改动被整体撤回，重新按接口约定交付。

\textbf{相关 PR：}\href{https://github.com/214notfound/jiedeng/pull/2}{账户模块 \#2}、\href{https://github.com/214notfound/jiedeng/pull/5}{剧情引擎与剧情内容板块 \#5}、\href{https://github.com/214notfound/jiedeng/pull/16}{剧情模块微调 \#16}、\href{https://github.com/214notfound/jiedeng/pull/19}{V1 收尾 \#19}。

\subsection{V2：剧情接口文档与展示适配}

V2 我的模块是 game-line，负责剧情数据与展示适配，工作包括核对 11 个剧情节点的展示类型与等待条件、把阅读完成与操作提交接到统一接口、按三个阶段做剧情回归。这一代的第一个 PR 交付的是剧情板块的接口文档，开发工作开始以接口为起点。

这一代集中暴露的是文档问题。同一件事由不同的人各写一份规则，两份界面切换规则合并时逐条比对，找出八处冲突；我自己交付的剧情接口文档同样经过反复调整。V2 后期团队开始用组合审阅处理并行提交，三组并行 PR 留下三份审阅文档，其中记录的问题大多不是实现错误，而是边界没有提前写明。

\textbf{相关 PR：}\href{https://github.com/214notfound/jiedeng/pull/23}{剧情板块接口文档 \#23}。

\subsection{V3：需求文档与剧情设定定稿}

V3 我承担本代需求文档的编写，内容包括 28 个剧情节点的清单、五条结局的接入顺序与 release-v3 的验收标准；剧情设定也在这一代定稿，五个结局的分支条件在这一步确定下来。此后我与罗晨菲同期参加数学建模竞赛，没有再承担 V3 的模块主责工作。

V3 的接口契约与 V1 那份被废弃的草案相比，差别在于前者把 14 条交接指令的发送方、接收方与携带字段全部写成了表格，并附注区分哪些写法属于交接层简写、哪些是正式实现。把一件事写清楚需要多少信息量，是 V1 阶段没有估计到的。

\textbf{相关 PR：}\href{https://github.com/214notfound/jiedeng/pull/63}{release-v3 合并 \#63}。

\subsection{V4：剧情文本定稿与数据清理}

V4 我负责剧情文本的定稿，46 处修改覆盖序幕、中段与结局，包括旁白、独白、NPC 对话的改写和角色签名的统一。

这一轮踩到三个陷阱，都不属于代码错误。某 NPC 的显示文本来自较新的数据文件，另一个文件里的同名内容早已失效；某个剧情节点被上层替换过，原文件里的两段旁白同样不再生效；选项的描述字段在归一化阶段被丢弃，写入后玩家不可见。三处问题的共同点是代码逻辑正确，修改的位置却不在生效路径上。

同期完成一项清理，测试中硬编码的块数、条数、末条 ID 改为从数据推导。剧情文本每调整一次都会牵动断言，写死数字的代价在这一轮集中体现出来。

\textbf{相关 PR：}\href{https://github.com/214notfound/jiedeng/pull/64}{剧情与对白修订 \#64}、\href{https://github.com/214notfound/jiedeng/pull/65}{剧情修改第二版 \#65}、\href{https://github.com/214notfound/jiedeng/pull/71}{剧情修改第二版 \#71}。\#70 与 \#71 内容相同，\#70 未合并，\#71 为最终合入的版本。

\subsection{回看这三周}

原创剧情与游戏形式的关系是这次最费力的部分。

《借灯》的故事线依靠人物的选择链条：陈晋年、王阙、白灯客三个名字下是同一个人，玩家最终要判断的不是相信谁，而是愿不愿意把三个名字下的行为放回同一个人身上。这条线在文本里可以靠叙述控制节奏，进入游戏后多出一层约束，玩家会按自己的顺序点击热点、翻看物品、跳过或重复阅读，任何一处都可能提前泄露后面的信息。

游戏形式本身也限制了剧情能怎么讲。项目是静态站点，没有配音和角色动画，可用的表达手段只有文字、图片、音乐和转场。哪些情绪交给文字，哪些交给画面，哪些留给音乐，需要在写文本的时候就分配好。V4 阶段转场、视频和音频接入之后，同一段文字在不同呈现方式下的效果差别明显，文本也需要跟着调整。

我的结论是，剧情文本从第一稿起就要按可点击、可跳过的阅读方式写，不能按纯文本的思路完成后直接搬进页面。

开发方式这件事的结论也一样。

PR 驱动的迭代方式在四个版本里保持了下来，五个人可以并行推进，冲突集中在合并时刻暴露，而不是积压到最后，这部分是有效的。

执行上的问题是配套没有跟上。提出时只想到要把需求写清楚，没有同时定义文件归属、接口字段和改动边界。V1 的接口草案因此被整份推翻，V2 出现两份界面规则文档合并时的八处冲突，到 V3 才形成可执行的接口契约。现在回头看，需求驱动的方向没有问题，缺的是让需求落地的中间层：需求文档之后必须有接口文档，接口文档必须写清谁发、谁收、带什么字段。这些定完，并行才真正成立。

\bigskip
{\small\color{black!55} 依据：仓库中的 GitHub PR 合并记录与相关提交，V1--V4 各代需求文档，V3 接口契约与剧情设定定稿。\par}

\section{高冰轩}

{\small\color{black!55}《借灯》网页交互故事 · 2026 年 9 月 3 日—9 月 19 日\par}

三周前，我对“完成一个功能”的理解还比较简单：页面能显示、按钮能点击，就觉得任务差不多结束了。随着《借灯》从 V1 推进到 V4，我逐渐发现，玩家看到的是一段连续的故事，开发时却要让剧情、探索、小游戏、素材、页面状态和存档在不同人负责的模块之间准确衔接。对我来说，这三周最大的成长，是开始从整个游玩流程判断自己的工作是否真正完成。

\subsection{V1：从单个功能入手，第一次面对团队接口}

V1 阶段，我主要制作了项目介绍、团队与成员页面，以及地图拼图小游戏。介绍页让我练习把内容、样式和页面结构组织起来；地图拼图则让我第一次认真处理“玩法规则”和“剧情结果”的边界：小游戏负责判断拼图是否完成，再通过约定的接口把结果交给主流程，而不是自行推进剧情。我还为拼图核心和适配器补了测试，开始意识到交付给队友的并不只是一个能打开的页面。

这一阶段也有一次很直接的教训：早期介绍页的合并需要撤回，再按接口约定调整、重新交付。它让我认识到，个人分支上看起来正确的实现，放进团队的公共页面后仍可能不合适。后来我会先确认文件归属、挂载位置和交接方式，再动手修改。

\textbf{相关 PR：}\href{https://github.com/214notfound/jiedeng/pull/7}{介绍页初版 \#7}、\href{https://github.com/214notfound/jiedeng/pull/8}{调整后的介绍页 \#8}、\href{https://github.com/214notfound/jiedeng/pull/10}{地图拼图 \#10}、\href{https://github.com/214notfound/jiedeng/pull/14}{介绍页完善 \#14}、\href{https://github.com/214notfound/jiedeng/pull/15}{拼图视觉更新 \#15}。其中 \#7 的合并后来被撤回，不能把它当作最终交付成果。

\subsection{V2：从“把内容放上去”走向“让玩家顺畅地操作”}

V2 的工作重心转到正式游戏页。我参与整理页面挂载区域、统一阅读组件、详情卡片、反馈提示和小游戏覆盖层，让剧情文字、NPC 对话、背包与探索信息能在同一页面中有序出现。阅读组件尤其让我体会到交互细节的重量：翻页不应提前提交剧情事实，关闭不能误触发完成；提交失败时，玩家需要看得懂的提示和可用的重试入口。地图入口、图片加载失败时的处理，以及窄屏阅读布局，也需要放到真实流程中验证。

V2 的联调过程并不总是顺利。不同 PR 组合时，样式规则可能互相覆盖，详情内容可能无法滚动，人物对白与旁白也可能挤成一段。我做过修复，也经历过改动被撤回后重新调整。逐渐学会了先复现问题、缩小修改范围，再用页面回归和测试确认没有伤到别人的模块。后期把分散的颜色和尺寸整理为共享样式变量，也是为了让各页面更容易保持一致。

\textbf{相关 PR：}\href{https://github.com/214notfound/jiedeng/pull/28}{游戏页反馈与资源降级 \#28}、\href{https://github.com/214notfound/jiedeng/pull/34}{阅读与页面交互收尾 \#34}、\href{https://github.com/214notfound/jiedeng/pull/36}{阅读与详情修复 \#36}、\href{https://github.com/214notfound/jiedeng/pull/39}{桌面阅读布局 \#39}、\href{https://github.com/214notfound/jiedeng/pull/40}{详情样式整理 \#40}、\href{https://github.com/214notfound/jiedeng/pull/43}{对白分段修复 \#43}、\href{https://github.com/214notfound/jiedeng/pull/45}{统一页面样式 \#45}。部分改动经过团队组合审阅后才进入版本，不能只凭单个 PR 的页面效果判断最终结果。

\subsection{V3：从负责一块页面，走向负责一条能跑通的链路}

V3 扩展了故事场景和结局，我的工作也从局部界面延伸到跨模块联调。我处理了地图拼图在正式流程中的入口和返回，修复游戏页初始化、登录界面与阅读出口的问题，参与接入 V3 正式剧情入口和全局状态。后续联调中，我重点检查探索热点、小游戏命令、结果事实与剧情节点是否能对上，并为重复结果、错误映射和移动端溢出补上约束或测试。背景音乐的播放、静音和跨页面续播，也是这一阶段补齐的体验细节。

这一阶段让我改变了验收标准。一个场景能显示，并不代表玩家能从前一节点进入、完成操作、保存结果，再走到下一节点。只有亲自沿着正式路径走一遍，并检查失败、返回和重复操作，才能判断链路是否可靠。我也开始更主动地读队友的接口文档和 PR，在冲突处讨论责任边界，而不是只守着自己写过的文件。

\textbf{相关 PR：}\href{https://github.com/214notfound/jiedeng/pull/49}{地图拼图正式流程整合 \#49}、\href{https://github.com/214notfound/jiedeng/pull/52}{初始化与登录修复 \#52}、\href{https://github.com/214notfound/jiedeng/pull/56}{正式剧情入口 \#56}、\href{https://github.com/214notfound/jiedeng/pull/57}{全局状态接入 \#57}、\href{https://github.com/214notfound/jiedeng/pull/58}{V3 探索联调 \#58}、\href{https://github.com/214notfound/jiedeng/pull/59}{入口与音频修复 \#59}、\href{https://github.com/214notfound/jiedeng/pull/61}{移动端布局 \#61}、\href{https://github.com/214notfound/jiedeng/pull/62}{发布验收 \#62}。\#58 是团队探索成果的合并，我主要参与其中的命令校验、映射修复与联调。

\subsection{V4：把三个小游戏从占位入口做成可交付的玩法}

V4 是我感受最深的一阶段。此前三个新小游戏有剧情入口和结果交接，但玩家还没有完整的操作过程。我先把需求拆成三种玩法：控制室的线路连通、矿井的移动探索、机房的证据收集与追逐；再把输入、移动、碰撞、镜头和游戏循环中稳定的部分抽成共享模块，保留各游戏独立的规则。实现后，我将它们接入原有 Gateway，并同步检查剧情提示、角色与场景素材、移动端显示和结果回传。

真正困难的地方在边界情况。线路看似连通却留有断头时不能判成功；追逐中连续碰撞不能让生命值一瞬间归零；玩家取消、技术错误和失败也不能被当成同一种结果。为此，我补了规则与接口测试，并处理退出后的监听器和动画清理。现在我更能理解，玩法“能玩”只是起点，让它能稳定地进入主线、正确地结束和返回，才是交付。9 月 19 日我又继续整理线路谜题的诊断和审阅信息，这部分是 \#68 合并后的分支后续改进，不计入该 PR 已合并的内容。

\textbf{相关 PR：}\href{https://github.com/214notfound/jiedeng/pull/68}{三套小游戏实现与接入 \#68}。

\subsection{回看这三周}

《借灯》让我从照着需求完成一个页面，走到能够追问“这个页面为什么这样运行、会不会影响前后流程”。我对 JavaScript 模块、页面状态、事件接口、Git 分支与 PR 协作都有了更具体的认识；也慢慢学会把 AI 辅助生成的代码当作需要自己理解、验证和负责的实现，而不是直接接受的答案。遇到问题时，最有效的办法常常是把现象缩小到一个明确的接口或状态，再和队友一起核对，而不是反复修改表面样式。

我也更喜欢现在的《借灯》：剧情、画面和玩法逐步接在一起后，它不再只是几个人分别完成的任务，而是一段玩家真的可以走进去的故事。仍有视觉细节和操作手感值得继续打磨，但这三周最重要的收获，是我开始具备了把自己的功能放进团队作品中、并为整段体验负责的意识。

\bigskip
{\small\color{black!55} 依据：仓库中的 GitHub PR 合并记录、相关提交及 V2/V3/V4 开发与验收文档；上文以个人实际改动为主，对团队 PR 明确标注参与范围。\par}

\section{卢正松}

\pending{本节待补充：按 V1 至 V4 分述承担的工作，末尾列相关 PR}

% 卢正松的可用事实（供撰写时参考）：
%   V1  探索与成就
%   V2  点击式探索、NPC 对话、背包与成就；文件边界为 assets/js/exploration、
%       assets/js/achievements 及对应样式，不修改剧情 Node、全局状态、正式
%       存档和 game.html 结构；E1 场景对象与热点配置、E2 点击调查与状态反馈、
%       E3 NPC 对话接入阅读组件、E4 线索详情与背包回读、E5 成就兼容与回归
%   V3  点击式探索与整体功能集成；组装 release-v3、主持完整流程测试、
%       判断问题归属并组织复测；场景热点坐标修正（陈家老宅四处）
%   V4  对话框设计；物品详情卡片与地图碎片的图片替换；场景图片细节微调
