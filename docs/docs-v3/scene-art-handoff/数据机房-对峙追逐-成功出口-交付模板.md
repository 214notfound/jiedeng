# 杨梦 V3 场景交付模板 — 数据机房（对峙追逐 · 成功出口）

## 1. 基本信息

| 字段 | 内容 |
| --- | --- |
| Node ID | `x-showdown` |
| 阶段 | `action` |
| 稳定 `targetId` | `x-showdown-chase`（chase，`completionMode:any`） |
| 正式 `sceneId` | `data-center`（已注册） |
| 允许结果 | `x-showdown-survived` |
| 覆盖拍点 | T15（成功出口） |
| 场景中文名 | 数据机房 / 成功出口（证据已外发） |
| 图片文件名/路径 | `数据机房-对峙追逐-成功出口-干净版-1280x720.svg` / `-标记草图-1280x720.svg` |
| 文件格式 | SVG 草图（成图转 PNG/WebP） |
| 像素尺寸 | 1280×720 |
| 是否包含人物 | 无（纯背景；小周为立绘覆盖层，非背景） |
| 版本与日期 | v1 · 2026-09-12 |

## 2. 关键对象标注（成功出口）

| 拍点 | 对象中文名 | 必须传达的信息 | 参考画布矩形 `x,y,w,h` | 建议热点空白区 | 是否可被裁切 | 卢正松最终 `objectId` |
| --- | --- | --- | --- | --- | --- | --- |
| T15 | 成功出口 | `x-showdown-survived`：证据外发，主角脱离 | — | — | 否 | 结果状态 |
| 小周 | 退后 | 退到一旁（立绘覆盖层，非背景） | — | — | 否 | `companion-x` |

## 3. 人物与立绘

| 人物 | 稳定 `npcId` | 出现方式 | 位置/朝向 | 是否必须可点击 | 不得提前透露的信息 |
| --- | --- | --- | --- | --- | --- |
| 小周 | `companion-x` | 立绘覆盖层（非背景），退后 | 左下 | 否 | 不画成施暴特写 |
| 主角 | 玩家角色 | 立绘覆盖层（非背景），脱离 | — | 否 | 不画具体受害画面 |

## 4. 安全区与裁切

| 视口 | 必须保留区域 | 非关键信息区（不裁切） | 阅读/按钮安全区 | 风险 |
| --- | --- | --- | --- | --- |
| 1280px | 全图 | 顶部 0–84、底部 600–720 | 底部 600–720 | 无 |
| 768px | 全图（contain） | 同上 | 底部 600–720 | 无 |
| 390px | 全图（contain） | 同上 | 底部 600–720 | 按钮/热点 ≥44px |

## 5. 视觉规范检查

- [x] 冷夜底色与冷绿/冷红屏幕光遵循 V3 UI 规范。
- [x] 成功用冷绿 `#7fd1c8` 为主光，语义清楚。
- [x] 屏幕显示「证据已外发」，一侧是打开的出口。
- [x] 纯背景，无人物、无人物剪影；小周/主角为立绘覆盖层。

## 6. 三方确认

| 责任人 | 确认内容 | 结论 | 日期 |
| --- | --- | --- | --- |
| 杨梦 | 成功出口构图、对象位置、裁切、对比度 |  |  |
| 卢正松 | `x-showdown-survived` 结果回传 |  |  |
| 高冰轩 | 追逐容器、结果页、覆盖层、焦点 |  |  |

## 7. 未决事项

- `sceneId`：`data-center` 已注册。
- 人物层：主角成功出口是否需「逃离背影」形象 → 待高冰轩（立绘覆盖层，非背景）。
- 音频与切换点：成功音 → 待定。
- 需要谁确认：卢正松（结果触发条件）、高冰轩（结果页承载）。
