# 杨梦 V3 场景交付模板 — 数据机房（对峙追逐 · 对峙入口）

## 1. 基本信息

| 字段 | 内容 |
| --- | --- |
| Node ID | `x-recovery-confrontation` |
| 阶段 | `action` |
| 稳定 `targetId` | `x-recovery-demand`（对话） |
| 正式 `sceneId` | `data-center`（已注册） |
| 稳定 `npcId` | `companion-x` |
| 覆盖拍点 | T12、T13（对峙入口） |
| 场景中文名 | 数据机房 / 对峙入口 |
| 图片文件名/路径 | `数据机房-对峙追逐-对峙入口-干净版-1280x720.svg` / `-标记草图-1280x720.svg` |
| 文件格式 | SVG 草图（成图转 PNG/WebP） |
| 像素尺寸 | 1280×720 |
| 是否包含人物 | 无（纯背景；小周为立绘覆盖层，非背景） |
| 版本与日期 | v1 · 2026-09-12 |

## 2. 关键对象标注（对峙入口）

| 拍点 | 对象中文名 | 必须传达的信息 | 参考画布矩形 `x,y,w,h` | 建议热点空白区 | 是否可被裁切 | 卢正松最终 `objectId` |
| --- | --- | --- | --- | --- | --- | --- |
| T12 | 供电范围/延时器/排水线 | 追逐机关（小游戏内交互） | 180,220,200,160 | 屏上方 | 否 | `TBD` |
| T13 | 安保封锁/副本删除压力 | 追逐机关（小游戏内交互） | 900,220,200,160 | 屏上方 | 否 | `TBD` |
| 小周 | 对话 `x-recovery-demand` | 目的说明：一路陪同、重启装置、监视调查，是要主角找齐并交回证据（立绘覆盖层） | 580,400,120,150 | 人物上方 | 否 | `companion-x` |

> 交证/拒绝按钮由剧情 `CHOICE_MADE` 负责，不写进对话模块；底部 600–720 为按钮安全区（归卢正松/高冰轩）。

## 3. 人物与立绘

| 人物 | 稳定 `npcId` | 出现方式 | 位置/朝向 | 是否必须可点击 | 不得提前透露的信息 |
| --- | --- | --- | --- | --- | --- |
| 小周 | `companion-x` | 立绘覆盖层（非背景），对话 | 中央 | 是（对话） | 只说明目的，不代做交证/拒绝选择 |
| 主角 | 玩家角色 | 立绘覆盖层（非背景） | — | 否 | 不画具体受害画面 |

## 4. 安全区与裁切

| 视口 | 必须保留区域 | 非关键信息区（不裁切） | 阅读/按钮安全区 | 风险 |
| --- | --- | --- | --- | --- |
| 1280px | 全图 | 顶部 0–84、底部 600–720 | 底部 600–720 | 无 |
| 768px | 全图（contain） | 同上 | 底部 600–720 | 无 |
| 390px | 全图（contain） | 同上 | 底部 600–720 | 按钮/热点 ≥44px |

## 5. 视觉规范检查

- [x] 冷夜底色与冷蓝/冷红屏幕光遵循 V3 UI 规范。
- [x] 屏幕冷蓝 `#5aa7ff`，小周立绘冷绿 `#7fd1c8`。
- [x] 交证/拒绝按钮文字与实际事实方向一致（不反向）。
- [x] 纯背景，无人物、无人物剪影；小周为立绘覆盖层。

## 6. 三方确认

| 责任人 | 确认内容 | 结论 | 日期 |
| --- | --- | --- | --- |
| 杨梦 | 对峙入口构图、对象位置、裁切、对比度 |  |  |
| 卢正松 | 对话 `x-recovery-demand`、T12/T13 机关、CHOICE_MADE |  |  |
| 高冰轩 | 对话容器、按钮、覆盖层（含小周立绘）、焦点 |  |  |

## 7. 未决事项

- `sceneId`：`data-center` 已注册。
- 追逐中间过程：归卢正松/高冰轩；杨梦只交付入口 + 成功/失败出口。
- 人物层：小周/主角均为立绘覆盖层（非背景）。
- 音频与切换点：追逐紧张音 → 待定。
- 需要谁确认：卢正松（二选一结果与《封井之人》触发条件）、高冰轩（对话容器）。
