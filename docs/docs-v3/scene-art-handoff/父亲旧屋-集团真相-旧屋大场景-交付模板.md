# 杨梦 V3 场景交付模板 — 父亲旧屋（集团真相 · 旧屋大场景层）

## 1. 基本信息

| 字段 | 内容 |
| --- | --- |
| Node ID | `father-company-truth` |
| 阶段 | `identity-reconstruction` |
| 稳定 `targetId` | `investigate-father-and-company` |
| 正式 `sceneId` | `father-house`（已注册） |
| 覆盖拍点 | F00（旧屋层：地窖口入口） |
| 场景中文名 | 父亲旧屋 / 旧屋大场景（环境层） |
| 图片文件名/路径 | `父亲旧屋-集团真相-旧屋大场景-干净版-1280x720.svg` / `-标记草图-1280x720.svg` |
| 文件格式 | SVG 草图（成图转 PNG/WebP） |
| 像素尺寸 | 1280×720 |
| 是否包含人物 | 无（纯背景） |
| 版本与日期 | v1 · 2026-09-12 |

## 2. 关键对象标注（旧屋层：仅地窖口可点）

| 拍点 | 对象中文名 | 必须传达的信息 | 参考画布矩形 `x,y,w,h` | 建议热点空白区 | 是否可被裁切 | 卢正松最终 `objectId` |
| --- | --- | --- | --- | --- | --- | --- |
| F00 | 地窖口/暗层入口 | 进入父亲旧屋暗层 | 560,500,160,70 | 口上方 | 否 | `TBD` |

> 旧屋层仅地窖口 1 个入口（点击高亮指示由 UI 覆盖层实现，不画入背景）；F01–F08 证据墙在暗层（另一张图）。

## 3. 人物与立绘

| 人物 | 稳定 `npcId` | 出现方式 | 位置/朝向 | 是否必须可点击 | 不得提前透露的信息 |
| --- | --- | --- | --- | --- | --- |
| 父亲 | 无 V3 对话 handoff ID | 旧物、文件、记忆，不画成实体人 | — | 否 | 必须同时表现最初受迫和后来主动毁证，不能单向洗白 |

## 4. 安全区与裁切

| 视口 | 必须保留区域 | 非关键信息区（不裁切） | 阅读/按钮安全区 | 风险 |
| --- | --- | --- | --- | --- |
| 1280px | 全图 | 顶部 0–90、底部 610–720 | 底部 610–720 | 无 |
| 768px | 全图（contain） | 同上 | 底部 610–720 | 无 |
| 390px | 全图（contain） | 同上 | 底部 610–720 | 热点 ≥44px |

## 5. 视觉规范检查

- [x] 冷夜底色、雨、雾与白灯暖光遵循 V3 UI 规范。
- [x] 关键对象和热点不与主白灯光晕混在一起。
- [x] 正文或标签所在区域对比度足够。
- [x] 地窖口为唯一暖光，屋体没入暗处。
- [x] 雨和光效不干扰阅读。
- [x] 纯背景，无人物、无人物剪影；父亲不画成实体人。

## 6. 三方确认

| 责任人 | 确认内容 | 结论 | 日期 |
| --- | --- | --- | --- |
| 杨梦 | 旧屋层构图、地窖口入口、裁切、对比度 |  |  |
| 卢正松 | 地窖口→暗层跳转绑定 |  |  |
| 高冰轩 | 两层页面承载、缩放、覆盖层、焦点 |  |  |

## 7. 未决事项

- `sceneId`：`father-house` 已注册；河边遗物与集团目录证据是否拆分 → 待定。
- 人物层：父亲是否需立绘/记忆闪回画面 → 待卢正松/高冰轩。
- 详情图/背包图：原始矿图、股权编号是否另交特写 → 待卢正松。
- 音频与切换点：暗层进入音 → 待定。
- 需要谁确认：卢正松（跳转绑定）、高冰轩（旧屋层页面承载）。
