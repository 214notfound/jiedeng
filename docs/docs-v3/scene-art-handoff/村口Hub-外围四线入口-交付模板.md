# 杨梦 V3 场景交付模板 — 村口 Hub（外围四线入口）

## 1. 基本信息

| 字段 | 内容 |
| --- | --- |
| Node ID | `outer-lines-investigation` |
| 阶段 | `outer-investigation` |
| 稳定 `targetId` | 本场景为四线入口 Hub，本身无独立 targetId；进入各子场景后才发对应 target |
| 正式 `sceneId` | `village`（已冻结） |
| 覆盖拍点 | 四个导航入口 G / R / S / W（非拍点编号，指向子场景） |
| 场景中文名 | 村口 Hub（外围四线入口） |
| 图片文件名/路径 | `村口Hub-外围四线入口-干净版-1280x720.svg` / `-标记草图-1280x720.svg` |
| 文件格式 | SVG 草图（成图转 PNG/WebP） |
| 像素尺寸 | 1280×720 |
| 是否包含人物 | 无（空旷雨夜，无独立立绘） |
| 版本与日期 | v1 · 2026-09-12 |

## 2. 关键对象标注

> 四个入口为**导航入口**（不占用 objectId）：点击触发对应子场景跳转，**不发送 `OBJECT_INVESTIGATED`、不产生调查完成事实**；进入子场景后调查具体对象，整线完成才由子场景回传结果。

| 拍点 | 对象中文名 | 必须传达的信息 | 参考画布矩形 `x,y,w,h` | 建议热点空白区 | 是否可被裁切 | 卢正松最终 `objectId` |
| --- | --- | --- | --- | --- | --- | --- |
| G | 山腰/墓地入口 | 通往山腰旧运输道（陈晋年线二） | 200,210,160,140 | 入口上侧天空 | 否 | 导航，不占用 objectId |
| R | 村委旧楼门禁 | 通往村委旧楼/临时项目部（王阙线一） | 900,180,180,160 | 门禁上方 | 否 | 导航，不占用 objectId |
| S | 废弃小学门 | 通往废弃小学（苏禾线） | 200,470,200,140 | 门上方 | 否 | 导航，不占用 objectId |
| W | 邮电所/拒签户 | 通往废弃邮电所（白灯客线一） | 890,470,200,140 | 门上方 | 否 | 导航，不占用 objectId |

## 3. 人物与立绘

| 人物 | 稳定 `npcId` | 出现方式 | 位置/朝向 | 是否必须可点击 | 不得提前透露的信息 |
| --- | --- | --- | --- | --- | --- |
| 无 | — | — | — | — | 本 Hub 不出现人物，避免任何身份/责任提前泄漏 |

## 4. 安全区与裁切

| 视口 | 必须保留区域 | 非关键信息区（不裁切） | 阅读/按钮安全区 | 风险 |
| --- | --- | --- | --- | --- |
| 1280px | 全图 | 顶部 0–60（返回键）、底部 600–720（阅读/对话） | 底部 600–720 | 无 |
| 768px | 全图（contain 等比缩放） | 同上 | 底部 600–720 | 无 |
| 390px | 全图（contain 等比缩放） | 同上，覆盖层相对更密集 | 底部 600–720 | 入口仍 ≥44px |

## 5. 视觉规范检查

- [x] 冷夜底色、雨、雾与白灯暖光遵循 V3 UI 规范。
- [x] 四个入口为导航入口；点击高亮由 UI 覆盖层实现，不画入背景；门口残灯暖光不与中央白灯光晕混在一起。
- [x] 正文或标签所在区域对比度足够。
- [x] 朱砂红只用于关键真相/错误/门外声音，本图不用。
- [x] 雨和光效不干扰阅读，减少动态效果时可以停止。
- [x] 背景中没有提前泄露后续身份或责任的姓名、正脸、文件标题。

## 6. 三方确认

| 责任人 | 确认内容 | 结论 | 日期 |
| --- | --- | --- | --- |
| 杨梦 | 构图、对象位置、裁切、对比度、视觉信息 |  |  |
| 卢正松 | 入口导航绑定、子场景跳转、不误发事实 |  |  |
| 高冰轩 | 容器比例、缩放、覆盖层、焦点与页面路由 |  |  |

## 7. 未决事项

- `sceneId`：`village` 已冻结；四个子场景正式 `sceneId` 仍为 TBD。
- 入口交互 = 导航（scene 跳转，不发 `OBJECT_INVESTIGATED`）→ 已定；页面承载方式 → 高冰轩。
- 对象拆分：四个入口不占用 objectId；子场景内对象由各自模板定义。
- 人物层：无。
- 详情图/背包图：无。
- 音频与切换点：入口切换音（雨夜底噪）→ 建议交杨梦，具体循环点待定。
- 需要谁确认：高冰轩（子场景路由与返回栈）、卢正松（导航与事实回传边界）。
